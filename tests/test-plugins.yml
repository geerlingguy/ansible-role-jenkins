---
- hosts: all
  name: Install Jenkins With Some Plugins
  become: true # FixMe: ez csak nekünk kell a vagrant miatt!!!!
  vars:
    jenkins_plugins:
      - blueocean
      - ghprb
      - greenballs
      - workflow-aggregator
    jenkins_plugin_timeout: 120

  pre_tasks:
    - include: java-8.yml

  roles:
    - geerlingguy.java
    - ansible-role-jenkins

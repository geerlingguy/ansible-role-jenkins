---
- hosts: all

  vars:
    jenkins_plugins:
      - blueocean
      - ghprb
      - greenballs
      - workflow-aggregator
      - { name: cloudbees-folder, version: 6.2.1 } # Validate specific version, latest is 6.3 as of this writing.
    jenkins_plugin_timeout: 120

  pre_tasks:
    - include: java-8.yml

  roles:
    - geerlingguy.java
    - role_under_test
